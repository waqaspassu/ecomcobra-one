
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Configuration {
  id String @id @default(cuid())
  width Int
  height Int
  cropedImageUrl String?
  imageUrl String
}
